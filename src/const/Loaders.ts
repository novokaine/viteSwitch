export enum FETCH_STATE {
  IDLE = "idle",
  LOADING = "loading",
  SUCCESS = "success",
  ERROR = "ERROR",
  UNAUTHENTICATED = "unauthenticated"
}

export const { IDLE, LOADING, SUCCESS, ERROR, UNAUTHENTICATED } = FETCH_STATE;
