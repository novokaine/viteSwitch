services:
  frontend:
    container_name: ${APP_CONTAINER_NAME:-photo-delivery-frontend}
    build:
      context: .
      dockerfile: Dockerfile
      args:
        VITE_API_URL: ${VITE_API_URL}
    ports:
      - "${APP_PORT:-3000}:3000"
    restart: unless-stopped
